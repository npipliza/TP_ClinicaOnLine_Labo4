import { DirecTitulosDirective } from './direc-titulos.directive';

describe('DirecTitulosDirective', () => {
  it('should create an instance', () => {
    const directive = new DirecTitulosDirective();
    expect(directive).toBeTruthy();
  });
});
