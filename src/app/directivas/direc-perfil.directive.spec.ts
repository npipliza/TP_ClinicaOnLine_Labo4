import { DirecPerfilDirective } from './direc-perfil.directive';

describe('DirecPerfilDirective', () => {
  it('should create an instance', () => {
    const directive = new DirecPerfilDirective();
    expect(directive).toBeTruthy();
  });
});
