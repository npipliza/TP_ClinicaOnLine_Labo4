import { DirecTurnosDirective } from './direc-turnos.directive';

describe('DirecTurnosDirective', () => {
  it('should create an instance', () => {
    const directive = new DirecTurnosDirective();
    expect(directive).toBeTruthy();
  });
});
