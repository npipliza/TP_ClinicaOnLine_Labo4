import { FormatoFechaPipe } from './fto-fecha.pipe';

describe('FormatoFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
